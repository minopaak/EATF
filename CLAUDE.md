# 코드 작성 규칙

## 기본 원칙
- 가장 단순하고 읽기 쉬운 방법으로 작성한다.
- 기본 기능(for, if, list, dict 등)으로 충분하면 그걸 쓴다. 불필요한 라이브러리나 추상화는 도입하지 않는다.
- 단, 같은 동작을 더 적은 코드로 명확하게 표현할 수 있으면(내장 함수, 컴프리헨션, 벡터 연산 등) 그쪽을 쓴다.
- 단순함과 간결함이 충돌하면 읽기 쉬운 쪽을 택한다.

## 피한다
- 한 번만 쓰는 헬퍼 함수, 불필요한 클래스나 상속
- 이해하기 어려운 한 줄 트릭
- 요청하지 않은 예외처리, 로깅, 설정 옵션

## 따른다
- 표준 라이브러리와 내장 함수 우선
- 변수명은 짧고 의미가 드러나게
- 중복은 줄이되 억지로 일반화하지 않는다
- 주석은 "왜"가 필요할 때만 최소한으로
- 완성되고 바로 실행 가능한 코드로 작성한다
